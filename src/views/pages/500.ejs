<%- include('../partials/header') %>

<!-- 500 Hero -->
<section class="page-hero" style="min-height: 60vh;">
  <div class="container">
    <h1 class="page-hero-title" style="font-size: var(--text-7xl);">500</h1>
    <p class="page-hero-subtitle"><%= lang === 'vi' ? 'Đã xảy ra lỗi server.' : 'A server error has occurred.' %></p>
    <div style="margin-top: var(--space-8);">
      <a href="/?lang=<%= lang %>" class="btn btn-white btn-lg">
        <%= lang === 'vi' ? 'Về trang chủ' : 'Go Home' %>
      </a>
    </div>
  </div>
</section>

<%- include('../partials/footer') %>
<%- include('../partials/contact-widget') %>
